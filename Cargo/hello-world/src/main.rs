/** # Teste basico */
fn main() {
    let mut x:u64 = 45; // u64 tipo que não suporta valores negativos (diferente dp i64)
    let mut f: f64 = 0.5;

    print!("{}",x);
    
    x = 40;
    print!("{}",x);
}
